// Remember to run `npx webpack --mode=development` everytime editing this file

import React from 'react';
import ReactDOM from 'react-dom';
window["React"] = React;
window["ReactDOM"] = ReactDOM;

import NumberFormat from 'react-number-format';
window["number-format"] = NumberFormat;

// Doc of component in https://material-ui.com/components/{component}/ like https://material-ui.com/components/tabs/
import Button from '@material-ui/core/Button';
window["Button"] = Button;
